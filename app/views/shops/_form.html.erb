<%= form_for @shop do |f| %>
  <%= render 'shared/error_messages', resource: @shop %>

  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, class: 'form-control' %>
  </div>

  <div class="row">
    <fieldset class="col-md-6">
      <legend>Correios</legend>
      <div class="checkbox">
        <label>
          <%= f.check_box :forward_to_correios %>
          <%= Shop.human_attribute_name(:forward_to_correios) %>
        </label>
      </div>
      <div class="form-group">
        <%= f.label :correios_code %>
        <%= f.text_field :correios_code, class: 'form-control' %>
      </div>
      <div class="form-group">
        <%= f.label :correios_password %>
        <%= f.text_field :correios_password, class: 'form-control' %>
      </div>
      <div class="form-group">
        <%= f.label :correios_services %>
        <% Correios::SERVICES.each do |id, name| %>
          <div class="checkbox">
            <%= check_box_tag 'shop[correios_services][]', id,
                              @shop.correios_services.include?(id.to_i),
                              class: 'switch' %>
            <%= name %>
          </div>
        <% end %>
      </div>

      <div class="form-group">
        <%= f.label :normal_shipping_name %>
        <%= f.text_field :normal_shipping_name, class: 'form-control' %>
      </div>

      <div class="form-group">
        <%= f.label :express_shipping_name %>
        <%= f.text_field :express_shipping_name, class: 'form-control' %>
      </div>

    </fieldset>
    <fieldset class="col-md-6">
      <legend>Axado</legend>
      <div class="checkbox">
        <label>
          <%= f.check_box :forward_to_axado %>
          <%= Shop.human_attribute_name(:forward_to_axado) %>
        </label>
      </div>
      <div class="form-group">
        <%= f.label :axado_token %>
        <%= f.text_field :axado_token, class: 'form-control' %>
      </div>
    </fieldset>
  </div>

  <div class="form-group">
    <%= f.submit 'Salvar', class: 'btn btn-primary' %>
    <%= link_to 'Cancelar', shops_path, class: 'btn btn-default' %>
  </div>
<% end %>
